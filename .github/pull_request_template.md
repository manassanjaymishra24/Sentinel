## Description

Provide a summary of changes and reference related issues.

Fixes #(issue)

## Checklist

- [ ] Conforms to PEP 604 type annotations (`python -m mypy sentinel/ --strict` passes)
- [ ] Formatted with Ruff (`python -m ruff format .`)
- [ ] Added or updated test coverage in `tests/` (`pytest tests/ -v` passes)
- [ ] Verified no new external dependencies are introduced without team discussion
