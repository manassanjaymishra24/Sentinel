name: CI (Lint, Test, Security)

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint & Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Set up Python
        uses: actions/setup-python@f677139edd7d925edc7592245015c4d73da5ecf9
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: pip install ruff mypy

      - name: Run ruff check
        run: ruff check .

      - name: Run ruff format check
        run: ruff format --check .

      - name: Run mypy type checking
        run: mypy sentinel/ --strict

  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    needs: lint
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
      fail-fast: true
    env:
      ANTHROPIC_API_KEY: "test-key-sentinel"
      OPENAI_API_KEY: "test-key-sentinel"
      SENTINEL_ENV: "test"
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@f677139edd7d925edc7592245015c4d73da5ecf9
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Run pytest with coverage
        run: >
          pytest tests/ -v --tb=short
          --cov=sentinel --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@5ecb98a3c6b747ed38dc09f064a32470195c3c15
        with:
          files: ./coverage.xml
          flags: unittests
          name: Python-${{ matrix.python-version }}
          fail_ci_if_error: false

  security-scan:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Set up Python
        uses: actions/setup-python@f677139edd7d925edc7592245015c4d73da5ecf9
        with:
          python-version: "3.10"
          cache: pip

      - name: Install security tools
        run: pip install pip-audit bandit safety

      - name: Run pip-audit for dependency vulnerabilities
        run: pip-audit --desc

      - name: Run bandit for code security issues
        run: bandit -r sentinel/ -ll -f json -o bandit-report.json

      - name: Check bandit results
        run: |
          if grep -q '"severity": "HIGH"' bandit-report.json || grep -q '"severity": "MEDIUM"' bandit-report.json; then
            echo "Security issues found!"
            cat bandit-report.json
            exit 1
          fi

      - name: Run safety check
        run: safety check --json || true

      - name: Upload security reports
        uses: actions/upload-artifact@6ca3e1589f5700e67145017b4c6bcb8e0303ae3e
        if: always()
        with:
          name: security-reports
          path: bandit-report.json

  type-coverage:
    name: Type Coverage Report
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Set up Python
        uses: actions/setup-python@f677139edd7d925edc7592245015c4d73da5ecf9
        with:
          python-version: "3.10"
          cache: pip

      - name: Install mypy
        run: pip install mypy

      - name: Run mypy with report
        run: mypy sentinel/ --strict --txt-report mypy-report --html-report mypy-html || true

      - name: Check type coverage threshold
        run: |
          # Count fully typed vs total functions (simplified check)
          total=$(find sentinel -name "*.py" -type f | wc -l)
          echo "Total Python files in sentinel: $total"
          if [ "$total" -lt 5 ]; then
            echo "Not enough sentinel files - check directory structure"
            exit 1
          fi

      - name: Upload type coverage report
        uses: actions/upload-artifact@6ca3e1589f5700e67145017b4c6bcb8e0303ae3e
        with:
          name: type-coverage
          path: mypy-report/
